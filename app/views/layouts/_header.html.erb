<nav class="navbar navbar-default" id="head" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to content_tag(:span, 'Simpel', class: 'wide') +
                  content_tag(:span, 'Blog', class: 'thin'),
                  root_path, class: "navbar-brand", id: "brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
        <li>
          <%= link_to new_post_path do %>
            <i class="fa fa-pencil"></i> New Post
          <% end %>
        </li>
        <li>
          <%= link_to destroy_user_session_path, :method => :delete do %>
            <i class="fa fa-sign-out"></i> Logout
          <% end %>
        </li>
        <% else %>
        <li>
          <%= link_to "Login", "#login", "data-toggle" => "modal", :class => 'btn btn-small' %>
        </li>
        <li>
          <%= link_to "Sign Up", "#signup", "data-toggle" => "modal", :class => 'btn btn-small' %>
        </li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
